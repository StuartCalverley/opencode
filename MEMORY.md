# Global Memory

## Rules

- All custom skills we create must live under `~/.config/opencode/skills/` (global skills location).
- When looking for anything related to the global opencode directory, only look in `~/.config/opencode`.
